import { describe, it, expect } from 'vitest';

describe('Infraestrutura de testes', () => {
  it('deve executar testes unitários puros corretamente', () => {
    expect(1 + 1).toBe(2);
    expect(true).toBe(true);
  });
});
